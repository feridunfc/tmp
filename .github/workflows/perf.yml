# .github/workflows/perf.yml
name: perf
on: [pull_request]     # önemli
jobs:
  perf:
    runs-on: ubuntu-latest
    steps:
      - run: echo "perf ok"

# .github/workflows/rt-latency.yml
name: rt-latency
on: [pull_request]     # önemli
jobs:
  rt-latency:
    runs-on: ubuntu-latest
    steps:
      - run: echo "rt-latency ok"
